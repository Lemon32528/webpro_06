# 開発者用仕様書（仮）

## ToDoリスト機能

### データ構造

項目名 | 型 | 内容
-|-|-
id | 数値 | 識別用のID
title | 文字列 | タスク名
deadline | 文字列 | タスクの期限
status | 文字列 | 進行状況

### ページ遷移
```mermaid
stateDiagram-v2
    [*] --> 一覧表示
    一覧表示 --> 詳細表示:タスク名からリンク
    詳細表示 --> 編集画面: 編集ボタン
    編集画面 --> 登録
    登録 --> 一覧表示
    一覧表示 --> 追加画面: 追加ボタン
    追加画面 --> 登録
    詳細表示 --> 削除画面: 削除ボタン
    削除画面 --> 一覧表示
```

### HTTPメソッドとリソース名の一覧

HTTPメソッド | リソース名 | 機能概要 | 機能の詳細
-|-|-|-
GET | /todos | 一覧表示 | ToDoリストを表示する
GET | /todos/:id | 詳細表示 | 特定のタスクの詳細を表示する
GET | /todos/create | 新規登録フォーム | 新規登録用のフォームを表示する
POST | /todos | 新規登録処理 | 入力内容を保存する
GET | /todos/edit/:id | 編集フォーム | 既存データの編集用フォームを表示する
POST | /todos/update/:id | 更新処理 | 編集内容を上書き保存する
GET | /todos/delete/:id | 削除処理 | 特定のタスクを削除する

## 家計簿

## データ構造
項目名 | 型 | 内容
-|-|-
id | 数値 | 識別用のID
data | 文字列 | 利用日
category | 文字列 | 分類
item | 文字列 | 品目・内容
price | 数値 |　金額

### ページ遷移
```mermaid
stateDiagram-v2
    [*] --> 一覧表示
    一覧表示 --> 詳細表示:分類名からリンク
    詳細表示 --> 編集画面: 編集ボタン
    編集画面 --> 登録
    登録 --> 一覧表示
    一覧表示 --> 追加画面: 追加ボタン
    追加画面 --> 登録
    詳細表示 --> 削除画面: 削除ボタン
    削除画面 --> 一覧表示
```
### HTTPメソッドとリソース名の一覧
HTTPメソッド | リソース名 | 機能概要 | 機能の詳細
-|-|-|-
GET | /kakeibo | 一覧表示 | 支出一覧画面を表示
GET | /kakeibo/:id | 詳細表示 | 特定の支出データの詳細を表示する
GET | /kakeibo/create | 新規登録フォーム | 新規登録用のフォームを表示する
POST | /kakeibo | 新規登録処理 | 入力内容を保存する
GET | /kakeibo/edit/:id | 編集フォーム | 既存データの編集用フォームを表示する
POST | /kakeibo/update/:id | 更新処理 | 編集内容を上書き保存する
GET | /kakeibo/delete/:id | 削除処理 | 特定の支出データを削除する

### サブスク管理

### データ構造

項目名 | 型 | 内容
-|-|-
id | 数値 | 識別用のID
title | 文字列 | タスク名
deadline | 文字列 | タスクの期限
status | 文字列 | 進行状況

### ページ遷移

```mermaid
stateDiagram-v2
    [*] --> 一覧表示
    一覧表示 --> 詳細表示:分類名からリンク
    詳細表示 --> 編集画面: 編集ボタン
    編集画面 --> 登録
    登録 --> 一覧表示
    一覧表示 --> 追加画面: 追加ボタン
    追加画面 --> 登録
    詳細表示 --> 削除画面: 削除ボタン
    削除画面 --> 一覧表示
```

### HTTPメソッドとリソース名の一覧

HTTPメソッド | リソース名 | 機能概要 | 機能の詳細
-|-|-|-
GET | /subs | 一覧表示 | 支出一覧画面を表示
GET | /sub/:id | 詳細表示 | 特定の支出データの詳細を表示する
GET | /subs/create | 新規登録フォーム | 新規登録用のフォームを表示する
POST | /subs | 新規登録処理 | 入力内容を保存する
GET | /kakeibo/edit/:id | 編集フォーム | 既存データの編集用フォームを表示する
POST | /kakeibo/update/:id | 更新処理 | 編集内容を上書き保存する
GET | /kakeibo/delete/:id | 削除処理 | 特定の支出データを削除する
